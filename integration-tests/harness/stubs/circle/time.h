//
// Host-build stub for <circle/time.h>. Only time_t is used, so defer to the
// host's own <time.h> rather than restating CTime.
//
#ifndef _circle_time_h
#define _circle_time_h

#include <time.h>

#endif
