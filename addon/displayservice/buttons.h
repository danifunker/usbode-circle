#ifndef BUTTONS_H
#define BUTTONS_H

// An enum representing the buttons
enum class Button
{
    Up,
    Down,
    Left,
    Right,
    Cancel,
    Ok,
    Count // sentinal value - keep this as the last entry!!!
};

#endif
